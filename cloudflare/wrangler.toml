# Pay2X API Gateway - Cloudflare Worker Config
name = "pay2x-api"
main = "api-worker.js"
compatibility_date = "2024-01-01"

# Custom domain (add after deploying)
# routes = [
#   { pattern = "api.pay2x.io/*", zone_name = "pay2x.io" }
# ]

[vars]
ENVIRONMENT = "production"
